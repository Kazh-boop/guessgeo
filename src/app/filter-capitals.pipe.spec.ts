import { FilterCapitalsPipe } from './filter-capitals.pipe';

describe('FilterCapitalsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCapitalsPipe();
    expect(pipe).toBeTruthy();
  });
});
